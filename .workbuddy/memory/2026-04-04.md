# 2026-04-04 工作日志

## MyMind App 开发进展

**项目路径：** `e:\MyMind`
**包名：** `com.example.mymind`

### 本次完成的工作

1. **MindNode 实体升级**：添加 `noteId` 字段，支持节点绑定笔记
2. **AppDatabase 版本升级 1→2→3**：添加正式 Migration（`MIGRATION_1_2`、`MIGRATION_2_3`），不再依赖 fallbackToDestructiveMigration
3. **思维导图↔笔记双向联动**：节点点击弹出"新建/选择已有笔记"对话框，长按显示操作菜单（编辑/删除/绑定/解绑/打开笔记）
4. **AI 模块**：Retrofit + DeepSeek API（OpenAI 兼容格式）；AiGenerateDialog 支持笔记生成和思维导图节点生成；AiConfig 用 SharedPreferences 持久化配置
5. **回收站 Activity**：TrashActivity + TrashAdapter + TrashViewModel，Tab 切换笔记/思维导图，支持恢复、永久删除、清空
6. **WorkManager 自动清理**：TrashCleanupWorker 每天执行，清理超过 15 天的回收站数据
7. **主界面菜单**：顶部工具栏 overflow 菜单，含"回收站"和"AI 设置"入口
8. **滑动删除**：NoteListFragment 和 MindMapListFragment 均支持左/右滑移入回收站，Snackbar 撤销
9. **蓝色主题**：Material 3 蓝色系配色（`#1565C0`），支持深色模式
10. **平板适配**：`layout-sw600dp/activity_main.xml` 使用 NavigationRailView 侧边导航；`layout-sw600dp/activity_note_editor.xml` 编辑区居中 80% 宽度
11. **修复 parseAndAddAiNodes 重复添加 bug**

### 当前数据库版本：3

### 关键文件路径
- 主 Activity: `app/src/main/java/com/example/mymind/MainActivity.kt`
- 数据库: `data/local/AppDatabase.kt`（v3，含 Migration）
- 回收站: `ui/trash/TrashActivity.kt`
- AI 网络层: `network/AiRetrofitClient.kt`, `AiApiService.kt`, `AiConfig.kt`
- 平板布局: `res/layout-sw600dp/activity_main.xml`
